# seeso-commit-and-deploy

커밋 및 배포 자동화를 위한 Claude Code 플러그인입니다.

## 기능

- **인프라 자동 감지**: 프로젝트의 배포 인프라를 자동으로 감지
- **작업 내용 분석**: 변경 내용을 분석하여 Hotfix/Bugfix/Feature 판단
- **배포 전략 결정**: 분석 결과를 바탕으로 Staging/Production 배포 범위 결정
- **코드 품질 검사**: Mock 코드, TODO, 디버그 코드 검출
- **브랜치 자동 관리**: 임시 브랜치 생성 → 커밋 → 머지 자동화
- **사용자 확인**: 자동 판단 후 사용자 확인 및 수정 가능

## 지원 인프라

### Frontend
- Vercel
- AWS Amplify
- Netlify
- Firebase Hosting
- Self-hosted (Docker)

### Backend
- AWS ECS/EC2 (GitHub Actions)
- Kubernetes
- Terraform + AWS
- Serverless Framework
- Fly.io
- Render
- Docker Compose

### CI/CD
- GitHub Actions
- GitLab CI
- Jenkins
- CircleCI

## 설치

```bash
# 마켓플레이스 추가
/plugin marketplace add SeeSoRuFree/seeso-marketplace

# 플러그인 설치
/plugin install seeso-commit-and-deploy@seeso
```

## 사용법

```bash
/seeso-commit-and-deploy:run
```

### 실행 플로우

```
1. 인프라 확인 (캐시 또는 자동 감지)
   └─ .seeso-deploy.json 또는 프로젝트 설정 파일 분석

2. 변경 내용 분석
   ├─ 변경 파일 목록
   ├─ 변경 패턴 (에러 수정, 새 기능 등)
   ├─ 민감 영역 체크
   └─ 코드 품질 검사

3. 1차 판단 출력
   ├─ 작업 성격 (Hotfix/Bugfix/Feature)
   ├─ 배포 범위 (Staging/Production)
   └─ 검사 결과 (Mock/TODO/Debug)

4. 사용자 확인 & 수정
   └─ 자연어로 수정 가능

5. 브랜치 분리 & 커밋
   └─ 임시 브랜치 생성 → 커밋 → 타겟 브랜치 머지

6. 배포 실행
   └─ 인프라별 배포 전략 적용
```

### 예시

```
🚀 Seeso Commit & Deploy

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 인프라 감지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Frontend:  Vercel (vercel.json)
Backend:   AWS EC2 ASG (terraform/ + GitHub Actions)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 변경 내용 분석
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
파일:      3개 변경 (+15줄, -5줄)
영역:      Backend만
패턴:      에러 핸들링 수정

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 1차 판단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
작업 성격:   🔴 Hotfix
배포 범위:   Production 직행 가능
검사 결과:   Mock 없음 ✅, TODO 없음 ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✏️  확인 & 수정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

위 판단이 맞나요? 수정이 필요하면 알려주세요.

> 맞아

✅ Production 배포를 시작합니다...
```

### 사용자 응답 예시

```bash
# 판단 수정
> "핫픽스야, 바로 프로덕션 배포해줘"
> "스테이징만 배포하면 됨"
> "TODO는 무시해도 돼"
> "백엔드만 배포해"

# 그대로 진행
> "맞아"
> "ㅇㅇ"
> "ok"
```

## 인프라 캐시

첫 실행 시 감지된 인프라를 `.seeso-deploy.json`에 저장하여 다음부터 빠르게 진행합니다.

```json
{
  "infrastructure": {
    "frontend": { "platform": "vercel", "directory": "." },
    "backend": { "platform": "aws-ec2-asg", "directory": "api" }
  },
  "git": {
    "deploy_author": "seeso user <partner@seeso.kr>"
  }
}
```

## 파일 구조

```
seeso-commit-and-deploy/
├── .claude-plugin/
│   └── plugin.json
├── commands/
│   └── run.md
├── skills/
│   └── infra-detect/
│       └── SKILL.md
├── strategies/
│   ├── vercel.md
│   ├── aws-ec2-asg.md
│   ├── kubernetes.md
│   ├── docker-compose.md
│   ├── netlify.md
│   └── serverless.md
└── README.md
```

## 작업 성격 판단 기준

| 성격 | 조건 |
|------|------|
| 🔴 Hotfix | 소규모(≤20줄), 에러 핸들링 수정, urgent 키워드 |
| 🟡 Bugfix | 중규모(20-100줄), 기존 로직 수정, fix 키워드 |
| 🟢 Feature | 대규모(>100줄), 새 파일 추가, feat 키워드 |

## 배포 범위 판단 기준

| 상황 | 배포 범위 |
|------|----------|
| Hotfix + 검사 통과 | Production 직행 가능 |
| Bugfix + 검사 통과 | Staging → Production |
| Feature | Staging Only |
| Mock/TODO 있음 | Staging Only (Production 불가) |

## 라이선스

MIT
